# Chave secreta do Django. Gere uma própria em desenvolvimento/produção:
# python -c "import secrets; print(secrets.token_urlsafe(48))"
DJANGO_SECRET_KEY=

# URL onde o frontend (Vite) roda. Usada para liberar CORS.
FRONTEND_URL=http://localhost:5173
